296,297c296,298
<     if (iterations > 0xFF) {
<         conf.current_sa = J1939_IDLE_ADDR;
---
>     if (conf.current_sa == 71) { return; }  // we're static now
>     if (iterations > J1939_IDLE_ADDR){
>         conf.current_sa = 71;
